services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: stillpoint
      POSTGRES_PASSWORD: stillpoint
      POSTGRES_DB: stillpoint
    ports: ["5432:5432"]
  npc-mind:
    build: .
    env_file: .env
    depends_on: [postgres]
    ports: ["8443:8443"]
